'use strict';

describe('Directive: menuButton', function () {

  // load the directive's module
  beforeEach(module('nbtc2015App'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
