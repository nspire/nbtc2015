'use strict';

describe('Service: blurbService', function () {

  // load the service's module
  beforeEach(module('nbtc2015App'));

  // instantiate service
  var blurbService;
  beforeEach(inject(function (_blurbService_) {
    blurbService = _blurbService_;
  }));

});
