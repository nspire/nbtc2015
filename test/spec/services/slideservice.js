'use strict';

describe('Service: slideService', function () {

  // load the service's module
  beforeEach(module('nbtc2015App'));

  // instantiate service
  var slideService;
  beforeEach(inject(function (_slideService_) {
    slideService = _slideService_;
  }));

});
