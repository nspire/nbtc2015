'use strict';

describe('Service: mainMenu', function () {

  // load the service's module
  beforeEach(module('nbtc2015App'));

  // instantiate service
  var mainMenu;
  beforeEach(inject(function (_mainMenu_) {
    mainMenu = _mainMenu_;
  }));

});
